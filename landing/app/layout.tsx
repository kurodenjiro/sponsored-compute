export const metadata = { title: 'Sponsored Compute — tài trợ hạ tầng cho AI agent' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="vi"><body style={{ margin: 0, background: '#0b0f19', color: '#e5e7eb', fontFamily: 'ui-sans-serif, system-ui' }}>{children}</body></html>;
}
