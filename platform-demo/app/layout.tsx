export const metadata = { title: 'Sponsored Compute · XSGD purpose-bound grants' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="vi"><body style={{ margin: 0 }}>{children}</body></html>;
}
