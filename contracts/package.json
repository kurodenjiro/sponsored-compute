{
  "name": "sponsored-compute-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network fuji",
    "deploy:avalanche": "hardhat run scripts/deploy.ts --network avalanche",
    "seed:fuji": "hardhat run scripts/seed.ts --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.5"
  }
}
